<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Inventario</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= active_menu_item(entries_path) %>"> <%= link_to('Ingresar Mercadería', entries_path) %> </li>
        <li class="<%= active_menu_item(sales_path) %>"> <%= link_to('Registrar Venta', sales_path) %> </li>
        <li class="<%= active_menu_item("#") %>"> <%= link_to('Inventario Base', stocks_path) %> </li>
        <li class="<%= active_menu_item(users_path) %>"> <%= link_to('Usuarios', users_path) %> </li>
        <li class="<%= active_menu_item("#") %>" class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Administrador
            <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li > <%= link_to('Tiendas', stores_path) %> </li>
            <li > <%= link_to('Artículos', items_path) %> </li>
            <li > <%= link_to('Cerrar Sesión', access_logout_path) %> </li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<% if !flash[:notice].blank? %>
  <div class="notice">
    <%= flash[:notice] %>
  </div>
<% end %>